sudo: required
language: java
jdk:
  - openjdk8
env:
  global:
    - IMAGE_NAME=bluecompute-orders
    - RELEASE_NAME=orders
    - TAG=travis-latest
    - HS256_KEY=
    - MYSQL_HOST=127.0.0.1
    - MYSQL_USER=root
    - MYSQL_DATABASE=ordersdb
    - MYSQL_PORT=3306
stages:
  - local build and test
  - docker build, deploy, and test
  - kubernetes build, deploy, and test
jobs:
  include:
    - stage: local build and test
      services:
      - mysql
      before_cache:
        - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
        - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
      cache:
        directories:
          - $HOME/.gradle/caches/
          - $HOME/.gradle/wrapper/
      before_script:
      # Wait MySQL
      - until mysql -h ${MYSQL_HOST} -P ${MYSQL_PORT} -uroot -e status; do echo "waiting for mysql"; sleep 1; done; echo "MySQL Started"
      # Create ordersdb database
      - mysql -u ${MYSQL_USER} -e "create database ${MYSQL_DATABASE}"
      script:
      # Gradle Build
      - ./gradlew build -x test
      # Start Application
      - java -Dspring.datasource.port="${MYSQL_PORT}" -Dspring.datasource.url="jdbc:mysql://${MYSQL_HOST}:${MYSQL_PORT}/${MYSQL_DATABASE}" -jar build/libs/micro-orders-0.0.1.jar &
      # Wait for the Orders container to start accepting connections
      - sleep 25
      # Run Orders API Test
      - bash scripts/api_tests.sh ${MYSQL_HOST} 8084 ${HS256_KEY}
    - stage: docker build, deploy, and test
      services:
      - mysql
      - docker
      install:
        - true
      before_script:
      # Wait MySQL
      - until mysql -h ${MYSQL_HOST} -P ${MYSQL_PORT} -uroot -e status; do echo "waiting for mysql"; sleep 1; done; echo "MySQL Started"
      # Create ordersdb database
      - mysql -u ${MYSQL_USER} -e "create database ${MYSQL_DATABASE}"
      script:
      # Build Docker image
      - docker build -t "${IMAGE_NAME}:${TAG}" .
      # Start Orders Container and Connect to local MySQL Service
      - docker run --net=host --name orders -d -p 8084:8084 -e MYSQL_HOST="${MYSQL_HOST}" -e MYSQL_PORT="${MYSQL_PORT}" -e MYSQL_USER="${MYSQL_USER}" -e HS256_KEY="${HS256_KEY}" "${IMAGE_NAME}:${TAG}"
      # Wait for the Orders container to start accepting connections
      - sleep 25
      # Check that the Orders container is running
      - docker ps
      # Check logs to see if it started properly
      - docker logs orders
      # Run Orders API Test
      - bash scripts/api_tests.sh ${MYSQL_HOST} 8084 ${HS256_KEY}
    - stage: kubernetes build, deploy, and test
      services:
      - docker
      env:
      - CHANGE_MINIKUBE_NONE_USER=true
      install:
        - true
      before_script:
      # Install nsenter, which is needed for minikube to work
      - bash scripts/install_minikube_and_helm.sh
      # Install MariaDB Chart
      - helm upgrade --install orders-mariadb --version 5.2.2 --set service.port=3307,nameOverride=orders-mariadb,rootUser.password=admin123,db.user=dbuser,db.password=password,db.name=${MYSQL_DATABASE},replication.enabled=false,master.persistence.enabled=false,slave.replicas=1,slave.persistence.enabled=false stable/mariadb
      script:
      # Build Docker image
      - docker build -t "${IMAGE_NAME}:${TAG}" .
      # Download Orders chart dependencies (MySQL)
      - cd chart/orders; helm dependency update; cd ../..
      # helm lint
      - helm lint chart/orders
      # helm package
      - helm package chart/orders
      # helm install Orders from packaged chart. Use pullPolicy=Never so it uses local docker image
      - CHART_VERSION="$(cat chart/orders/Chart.yaml | grep version | awk '{print $2}')"
      - helm upgrade --install orders --set service.type=NodePort,image.repository="${IMAGE_NAME}",image.tag="${TAG}",image.pullPolicy=Never orders-${CHART_VERSION}.tgz
      # Wait for Orders to be ready
      - kubectl get deployments ${RELEASE_NAME}-orders -o yaml
      - READY=$(kubectl get deployments ${RELEASE_NAME}-orders -o yaml | grep "readyReplicas" | awk '{print $2}')
      - echo $READY
      - until [ -n "$READY" ] && [ ${READY} -ge 1 ]; do READY=$(kubectl get deployments ${RELEASE_NAME}-orders -o yaml | grep "readyReplicas" | awk '{print $2}'); kubectl get deployments -o wide; echo "Waiting for orders to be ready"; sleep 10; done
      # Wait for Orders deployment to start accepting connections
      - sleep 35
      # Run Orders API Test
      - ${MINIKUBE_IP}=$(minikube ip)
      - ${NODE_PORT}=$(kubectl get service ${RELEASE_NAME}-orders -o=jsonpath='{.spec.ports[0].nodePort}')
      - bash scripts/api_tests.sh $${MINIKUBE_IP} $${NODE_PORT} ${HS256_KEY}